<h1>Doctor's Office Listing</h1>

<div class="row">
  <div class="col-md-6">
    <h3>Enter In Doctor Information</h3>
    <form action="/add_doctor" method="post">
      <div class="form-group">
        <label for="name">Doctor's Name:</label>
        <input type="text" name="first_name" required pattern=".*\S+.*" title="This field is required" placeholder="first">
        <input type="text" name="last_name" required pattern=".*\S+.*" title="This field is required" placeholder="last">
      </div>
      <div class="form-group">
        <label for="specialty_id" name="specialty_id">Specialty:</label>
        <select name="specialty_id">
          <option value="1">General Practice</option>
          <option value="2">Love Guru</option>
          <option value="3">Higher Thought</option>
        </select>
      </div>
      <button type="submit" name="button">Add</button>
    </form>

    <h2>Your Doctors</h2><br>
    <ul>
      <% @doctors.each do |doctor| %>
      <li><a href="/input/<%= doctor.id %>">Dr. <%= doctor.first_name %> <%= doctor.last_name %></a></li>
      <% end %>
    </ul>
  </div>



<div class="col-md-6">
  <h3>Enter in Patient Information</h3>
  <form action="/add_patient" method="post">
    <div class="form-group">
      <label for="name">Patient's Name:</label>
      <input type="text" name="first_name" required pattern=".*\S+.*" title="This field is required" placeholder="first">
      <input type="text" name="last_name" required pattern=".*\S+.*" title="This field is required" placeholder="last">
    </div>

    <div class="form-group">
    <label for="birthday">Enter Birthday:</label>
    <input type="date" id="birthday" name="birthday"
       value="2018-07-22"
       min="1900-01-01" max="2018-12-04">
    </div>

    <h2>Your Patients</h2><br>

    <form action="/add_patient">
      <label for="patients">Select a Doctor</lable>
      <select id='doctors' name="doctors" type='text'>
        <% @doctors.each() do |doctor| %>
          <option value="<%= doctor.id()%>"> <%= doctor.first_name() %> <%= doctor.last_name() %> </option>
        <% end %>
      </select>

    <!-- <div class="form-group">
      <label for="doctor_id" name="specialty_id">Specialty:</label>
      <select name="doctor_id">
        <option value="1">General Practice</option>
        <option value="2">Love Guru</option>
        <option value="3">Higher Thought</option>
      </select>
    </div> -->
    <button class="btn btn-success" type="submit" name="button">Add</button>
  </form>
</div>
